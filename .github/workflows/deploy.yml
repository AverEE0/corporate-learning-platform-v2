name: Deploy to Server

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 12  # Увеличено до 12 минут для пересборки без кеша
    concurrency:
      group: deploy
      cancel-in-progress: false  # Не отменять текущий деплой, если запущен новый
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
          echo "✅ SSH ключ настроен"
          echo "🔍 Проверка SSH подключения..."
          ssh -o ConnectTimeout=10 -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "echo 'SSH connection successful'" || {
            echo "❌ Ошибка SSH подключения"
            exit 1
          }
      
      - name: Deploy to server
        timeout-minutes: 10  # Таймаут для SSH сессии (увеличено для пересборки без кеша)
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          ssh -o ConnectTimeout=5 -o ServerAliveInterval=15 -o ServerAliveCountMax=2 -o BatchMode=yes $SSH_USER@$SSH_HOST << 'ENDSSH'
            set -e  # Exit on any error
            
            # Функция для логирования с временной меткой
            log() {
              echo "[$(date +'%Y-%m-%d %H:%M:%S')] $1"
            }
            
            # Функция для обработки ошибок
            handle_error() {
              local exit_code=$?
              local line_number=$1
              log "❌ ОШИБКА на строке $line_number (exit code: $exit_code)"
              log "📋 Последние команды:"
              history | tail -5 || true
              log "📁 Текущая директория: $(pwd)"
              log "🔍 Проверка окружения:"
              log "   - Git: $(git --version 2>/dev/null || echo 'не установлен')"
              log "   - Docker: $(docker --version 2>/dev/null || echo 'не установлен')"
              log "   - Docker Compose: $(docker compose version 2>/dev/null || echo 'не установлен')"
              log "   - Свободное место: $(df -h / | tail -1 | awk '{print $4}')"
              exit $exit_code
            }
            
            trap 'handle_error $LINENO' ERR
            
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            log "🚀 НАЧАЛО ДЕПЛОЯ"
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            log "📋 Информация о сервере:"
            log "   - Хост: $(hostname)"
            log "   - Пользователь: $(whoami)"
            log "   - Директория: $(pwd)"
            log ""
            
            # Шаг 1: Проверка и переход в директорию проекта
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            log "ШАГ 1: Проверка директории проекта"
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            if [ ! -d "/root/corporate-learning-platform-v2" ]; then
              log "⚠️  Директория не найдена, клонируем репозиторий..."
              cd /root || { log "❌ Не удалось перейти в /root"; exit 1; }
              
              log "📦 Клонирование репозитория..."
              if git clone https://github.com/AverEE0/corporate-learning-platform-v2.git 2>&1; then
                log "✅ Репозиторий успешно клонирован"
              else
                log "❌ Ошибка клонирования репозитория"
                log "   Проверьте:"
                log "   - Доступ к GitHub (репозиторий должен быть публичным или настроен доступ)"
                log "   - Интернет соединение на сервере"
                exit 1
              fi
            else
              log "✅ Директория существует: /root/corporate-learning-platform-v2"
            fi
            
            cd /root/corporate-learning-platform-v2 || { log "❌ Не удалось перейти в директорию проекта"; exit 1; }
            log "📁 Текущая директория: $(pwd)"
            log ""
            
            # Шаг 2: Настройка Git
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            log "ШАГ 2: Настройка Git"
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            
            # Проверка, что это git репозиторий
            if [ ! -d ".git" ]; then
              log "⚠️  Это не git репозиторий, инициализируем..."
              if git init 2>&1; then
                log "✅ Git репозиторий инициализирован"
              else
                log "❌ Ошибка инициализации git репозитория"
                exit 1
              fi
            fi
            
            # Настройка remote
            if ! git remote get-url origin > /dev/null 2>&1; then
              log "📦 Добавление git remote..."
              if git remote add origin https://github.com/AverEE0/corporate-learning-platform-v2.git 2>&1; then
                log "✅ Remote добавлен"
              else
                log "❌ Ошибка добавления remote"
                exit 1
              fi
            else
              log "🔄 Обновление git remote URL..."
              CURRENT_REMOTE=$(git remote get-url origin)
              log "   Текущий remote: $CURRENT_REMOTE"
              if git remote set-url origin https://github.com/AverEE0/corporate-learning-platform-v2.git 2>&1; then
                log "✅ Remote URL обновлен"
              else
                log "⚠️  Не удалось обновить remote, продолжаем..."
              fi
            fi
            
            log "🔗 Проверка remote:"
            git remote -v
            log ""
            
            # Шаг 3: Получение кода (оптимизировано для скорости)
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            log "ШАГ 3: Получение кода из GitHub"
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            
            log "📦 Выполнение git fetch (быстрый способ)..."
            # Используем --depth=1 для ускорения при необходимости
            if git fetch origin main --quiet 2>&1 || git fetch origin master --quiet 2>&1; then
              log "✅ Fetch выполнен успешно"
              
              log "🔄 Сброс на последний коммит..."
              if git reset --hard origin/main 2>&1; then
                log "✅ Сброс на origin/main выполнен"
              elif git reset --hard origin/master 2>&1; then
                log "✅ Сброс на origin/master выполнен"
              else
                log "⚠️  Сброс не удался, пробуем checkout..."
                git checkout -f main 2>/dev/null || git checkout -f master 2>/dev/null || true
                log "✅ Checkout выполнен"
              fi
            else
              log "⚠️  Git fetch не удался, пробуем pull..."
              if git pull origin main --quiet 2>&1; then
                log "✅ Pull из origin/main выполнен"
              elif git pull origin master --quiet 2>&1; then
                log "✅ Pull из origin/master выполнен"
              else
                log "❌ Не удалось получить код из GitHub"
                log "   Проверьте:"
                log "   - Доступ к GitHub"
                log "   - Правильность URL репозитория"
                log "   - Состояние git репозитория:"
                git status || true
                exit 1
              fi
            fi
            
            CURRENT_COMMIT=$(git rev-parse --short HEAD 2>/dev/null || echo 'unknown')
            CURRENT_MESSAGE=$(git log -1 --pretty=format:"%s" 2>/dev/null || echo 'unknown')
            log "📝 Текущий коммит: $CURRENT_COMMIT"
            log "📝 Сообщение коммита: $CURRENT_MESSAGE"
            log ""
            
            # Шаг 4: Проверка Docker
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            log "ШАГ 4: Проверка Docker"
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            
            if ! command -v docker > /dev/null 2>&1; then
              log "❌ Docker не установлен!"
              log "   Установите Docker: apt-get update && apt-get install -y docker.io"
              exit 1
            fi
            log "✅ Docker установлен: $(docker --version)"
            
            if ! command -v docker-compose > /dev/null 2>&1 && ! docker compose version > /dev/null 2>&1; then
              log "❌ Docker Compose не установлен!"
              log "   Установите: apt-get install -y docker-compose-plugin"
              exit 1
            fi
            log "✅ Docker Compose установлен: $(docker compose version 2>/dev/null || docker-compose --version)"
            
            # Проверка свободного места
            AVAILABLE_SPACE=$(df -h / | tail -1 | awk '{print $4}')
            log "💾 Свободное место: $AVAILABLE_SPACE"
            log ""
            
            # Шаг 5: Очистка кеша Next.js и сборка Docker контейнеров
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            log "ШАГ 5: Очистка кеша и сборка Docker контейнеров"
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            
            # АГРЕССИВНАЯ очистка кеша Next.js для принудительной пересборки
            log "🧹 АГРЕССИВНАЯ очистка кеша Next.js..."
            # Удаляем .next
            if [ -d ".next" ]; then
              rm -rf .next
              log "✅ Кеш Next.js (.next) удален"
            fi
            # Удаляем node_modules/.cache
            if [ -d "node_modules/.cache" ]; then
              rm -rf node_modules/.cache
              log "✅ Кеш node_modules удален"
            fi
            # Удаляем все временные файлы Next.js
            find . -type d -name ".next" -exec rm -rf {} + 2>/dev/null || true
            find . -type d -name ".swc" -exec rm -rf {} + 2>/dev/null || true
            # Очищаем Docker build cache для приложения
            log "🧹 Очистка Docker build cache..."
            docker builder prune -f 2>/dev/null || true
            log "✅ Docker build cache очищен"
            
            # Остановка старых процессов сборки, если они есть
            log "🛑 Проверка и остановка старых процессов сборки..."
            # Агрессивная очистка: убиваем ВСЕ процессы docker compose build
            pkill -9 -f 'docker compose' 2>/dev/null || true
            pkill -9 -f 'docker build' 2>/dev/null || true
            # Очистка зависших контейнеров сборки
            docker ps -a --filter "status=exited" --filter "name=build" -q | xargs -r docker rm -f 2>/dev/null || true
            sleep 2
            
            log "🏗️  Запуск docker compose build (ПРИНУДИТЕЛЬНАЯ ПЕРЕСБОРКА БЕЗ КЭША)..."
            log "   Используем --no-cache для гарантии свежей сборки"
            log "   Это займет больше времени, но гарантирует применение всех изменений"
            
            # Сборка с таймаутом (максимум 8 минут) и выводом прогресса
            log "⏱️  Таймаут сборки: 8 минут. Если превышен - деплой будет остановлен."
            log "📊 Запуск сборки..."
            
            # Принудительная пересборка БЕЗ кеша для гарантии применения изменений
            if timeout 480 docker compose build --no-cache --progress=plain app 2>&1 | tee /tmp/build.log; then
              log "✅ Сборка завершена успешно"
            else
              BUILD_EXIT_CODE=$?
              if [ $BUILD_EXIT_CODE -eq 124 ]; then
                log "❌ ТАЙМАУТ сборки Docker контейнера (превышен лимит 8 минут)"
                log "   Это означает, что сборка зависла или слишком долгая"
              else
                log "❌ Ошибка сборки Docker контейнера (код выхода: $BUILD_EXIT_CODE)"
              fi
              log "   Принудительная остановка всех процессов Docker..."
              pkill -9 -f 'docker compose' 2>/dev/null || true
              pkill -9 -f 'docker build' 2>/dev/null || true
              # Показываем последние строки лога для диагностики
              log "   Последние строки лога сборки:"
              tail -30 /tmp/build.log 2>/dev/null || echo "   Лог недоступен"
              log "   Проверьте:"
              log "   - docker-compose.yml файл"
              log "   - Dockerfile"
              log "   - Логи Docker: docker compose logs"
              log "   - Состояние: docker compose ps -a"
              exit 1
            fi
            log "✅ Сборка завершена успешно"
            log ""
            
            # Шаг 6: ПРИНУДИТЕЛЬНАЯ очистка и перезапуск
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            log "ШАГ 6: ПРИНУДИТЕЛЬНАЯ очистка и перезапуск"
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            
            log "🛑 АГРЕССИВНАЯ остановка всех контейнеров..."
            docker compose down app 2>/dev/null || true
            docker compose stop app 2>/dev/null || true
            docker compose rm -f app 2>/dev/null || true
            # Убиваем процессы внутри контейнера, если он еще работает
            docker kill corporate_learning_app 2>/dev/null || true
            docker rm -f corporate_learning_app 2>/dev/null || true
            sleep 3
            
            log "🧹 ФИНАЛЬНАЯ очистка кеша перед запуском..."
            rm -rf .next 2>/dev/null || true
            rm -rf node_modules/.cache 2>/dev/null || true
            find . -type d -name ".next" -exec rm -rf {} + 2>/dev/null || true
            find . -type d -name ".swc" -exec rm -rf {} + 2>/dev/null || true
            
            log "🚀 ПРИНУДИТЕЛЬНОЕ пересоздание и запуск контейнера..."
            if docker compose up -d --force-recreate --no-deps --build app 2>&1; then
              log "✅ Контейнер пересоздан и запущен"
            else
              log "❌ Ошибка запуска контейнеров"
              log "   Проверьте логи: docker compose logs app"
              log "   Проверьте состояние: docker compose ps"
              exit 1
            fi
            
            log "⏳ Ожидание запуска контейнера (5 секунд)..."
            sleep 5
            
            log "🔄 ПРИНУДИТЕЛЬНЫЙ перезапуск для применения изменений..."
            docker compose restart app 2>&1 || true
            sleep 3
            
            log ""
            
            # Шаг 7: Проверка статуса
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            log "ШАГ 7: Проверка статуса"
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            
            log "🧹 Очистка кеша Next.js ВНУТРИ контейнера..."
            docker compose exec -T app sh -c "rm -rf .next node_modules/.cache 2>/dev/null || true" 2>/dev/null || true
            log "✅ Кеш внутри контейнера очищен"
            
            log "🔄 ФИНАЛЬНЫЙ перезапуск после очистки кеша..."
            docker compose restart app 2>&1 || true
            sleep 5
            
            log "📊 Статус контейнеров:"
            docker compose ps 2>&1 || true
            
            log ""
            log "📋 Последние логи приложения (последние 50 строк):"
            docker compose logs --tail=50 app 2>&1 || true
            
            log ""
            log "🔍 Проверка версии кода в контейнере..."
            CURRENT_COMMIT_IN_CONTAINER=$(docker compose exec -T app git rev-parse --short HEAD 2>/dev/null || echo "unknown")
            log "   Коммит в контейнере: $CURRENT_COMMIT_IN_CONTAINER"
            log "   Ожидаемый коммит: $CURRENT_COMMIT"
            if [ "$CURRENT_COMMIT_IN_CONTAINER" = "$CURRENT_COMMIT" ]; then
              log "✅ Коммиты совпадают - новый код загружен!"
            else
              log "⚠️  Коммиты не совпадают - возможно, используется старый код"
            fi
            
            log ""
            log "🔍 Проверка здоровья приложения..."
            sleep 3
            if curl -f http://localhost:3044/api/health > /dev/null 2>&1; then
              log "✅ Приложение отвечает на /api/health"
            else
              log "⚠️  Приложение не отвечает на /api/health (может быть еще запускается)"
              log "   Попробуйте проверить через несколько секунд"
            fi
            
            log ""
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            log "✅ ДЕПЛОЙ ЗАВЕРШЕН УСПЕШНО!"
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            log "📝 Коммит: $CURRENT_COMMIT"
            log "📅 Время: $(date)"
            log "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          ENDSSH
