# CSRF Защита - Завершено

## ✅ Выполненные задачи

### CSRF (Cross-Site Request Forgery) Защита

#### 1. ✅ Серверная часть
- Создан модуль `lib/csrf.ts` с функциями:
  - `generateCSRFToken()` - генерация CSRF токена
  - `getCSRFToken()` - получение токена из cookie
  - `setCSRFToken()` - установка токена в cookie
  - `verifyCSRFToken()` - верификация токена (с защитой от timing attacks)
  - `requireCSRF()` - middleware для проверки CSRF
  - `getOrCreateCSRFToken()` - получение или создание токена

#### 2. ✅ Клиентская часть
- Создан модуль `lib/csrf-client.ts` с функциями:
  - `getCSRFToken()` - получение токена с сервера с кешированием
  - `clearCSRFTokenCache()` - очистка кеша токена
  - `addCSRFHeader()` - добавление CSRF заголовка к запросам
  - `fetchWithCSRF()` - wrapper для fetch с автоматическим CSRF

#### 3. ✅ API Endpoints
- `GET /api/csrf-token` - получение CSRF токена для клиента
- Интеграция CSRF проверки в:
  - `POST /api/courses` - создание курсов
  - `POST /api/auth/login` - возвращает CSRF токен при входе

#### 4. ✅ Middleware
- Создан `middleware.ts` для автоматической проверки CSRF на всех защищенных роутах
- Исключения для read-only методов (GET, HEAD, OPTIONS)
- Исключения для публичных путей (login, register, csrf-token)

#### 5. ✅ Безопасность
- Использование `crypto.timingSafeEqual()` для защиты от timing attacks
- HttpOnly cookies для CSRF токенов
- SameSite=strict для дополнительной защиты
- Secure флаг в production

### Особенности реализации

1. **Генерация токенов:**
   - Используется `crypto.randomBytes(32)` для криптографически стойких токенов
   - Токены хранятся в httpOnly cookies

2. **Верификация:**
   - Токен сравнивается из заголовка `x-csrf-token` и cookie `csrf-token`
   - Используется timing-safe сравнение для защиты от timing attacks

3. **Исключения:**
   - GET, HEAD, OPTIONS запросы не требуют CSRF (read-only)
   - Публичные endpoints (login, register) не требуют CSRF
   - Endpoint для получения CSRF токена не требует CSRF

4. **Интеграция:**
   - При входе пользователя автоматически генерируется CSRF токен
   - Клиент получает токен и кеширует его
   - Все POST/PUT/DELETE запросы автоматически включают CSRF заголовок

## 📁 Новые файлы

1. `lib/csrf.ts` - серверные функции для CSRF
2. `lib/csrf-client.ts` - клиентские функции для CSRF
3. `lib/csrf-middleware.ts` - middleware для проверки CSRF
4. `app/api/csrf-token/route.ts` - API для получения токена
5. `middleware.ts` - Next.js middleware для автоматической проверки

## 🔄 Измененные файлы

1. `app/api/courses/route.ts` - добавлена CSRF проверка в POST
2. `app/api/auth/login/route.ts` - возвращает CSRF токен при входе

## 🔒 Улучшения безопасности

- Защита от CSRF атак на все модифицирующие запросы
- Timing-safe сравнение токенов
- HttpOnly cookies для защиты от XSS
- SameSite=strict для защиты от CSRF через внешние сайты
- Secure флаг в production для защиты от перехвата

## 📝 Примечания

- Для использования CSRF в клиентском коде:
  ```typescript
  import { fetchWithCSRF } from '@/lib/csrf-client'
  
  // Вместо обычного fetch:
  const response = await fetchWithCSRF('/api/courses', {
    method: 'POST',
    body: JSON.stringify(data),
  })
  ```

- Для ручной проверки в API роуте:
  ```typescript
  import { requireCSRF } from '@/lib/csrf'
  
  const csrfCheck = await requireCSRF(request)
  if (!csrfCheck.success) {
    return NextResponse.json({ error: csrfCheck.error }, { status: 403 })
  }
  ```

