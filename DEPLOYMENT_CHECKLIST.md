# Чеклист перед копированием/деплоем проекта

## ✅ Перед копированием

### 1. База данных
- [ ] Все SQL миграции выполнены:
  - `scripts/01_create_database.sql` (основные таблицы)
  - `scripts/02_add_last_login.sql` (если нужно)
  - `scripts/03_add_audit_logs.sql` (аудит логи)
  - `scripts/04_add_notifications.sql` (уведомления)
  - `scripts/05_add_achievements.sql` (достижения и XP)
  - `scripts/06_add_forums.sql` (обсуждения)

### 2. Переменные окружения
- [ ] Создан файл `.env.local` с настройками:
  ```env
  DATABASE_URL=your_database_url
  JWT_SECRET=your_jwt_secret_key
  NODE_ENV=production
  ```
- [ ] Не добавлен `.env.local` в git (должен быть в `.gitignore`)

### 3. Зависимости
- [ ] Все зависимости установлены: `npm install`
- [ ] Проверена совместимость версий Node.js (рекомендуется Node.js 18+)

### 4. Сборка
- [ ] Проект собирается без ошибок: `npm run build`
- [ ] Нет критичных ошибок TypeScript

### 5. Тестовые данные
- [ ] Созданы тестовые пользователи (опционально):
  ```bash
  node scripts/create_test_users_with_db.js
  ```
- [ ] Создан хотя бы один админ пользователь для управления системой

## 📋 При копировании

### Файлы, которые НЕ нужно копировать:
- `node_modules/` - установится на новом месте
- `.next/` - соберется при деплое
- `.env.local` - создайте заново с нужными настройками
- `*.log` - логи не нужны
- `.git/` - только если не используете git для деплоя

### Файлы, которые ОБЯЗАТЕЛЬНО нужно скопировать:
- Все `.ts`, `.tsx`, `.js` файлы
- Все `.sql` файлы в `scripts/`
- `package.json` и `package-lock.json`
- `next.config.js` (если есть)
- `tailwind.config.js` (если есть)
- `tsconfig.json`
- Все файлы в `public/` (если есть статические файлы)
- Папка `components/`
- Папка `lib/`
- Папка `app/`
- Все конфигурационные файлы

## 🔧 После копирования

### 1. Установка зависимостей
```bash
npm install
# или
npm ci  # для точного воспроизведения
```

### 2. Настройка переменных окружения
- Создайте `.env.local` с правильными значениями
- Проверьте `DATABASE_URL`
- Проверьте `JWT_SECRET` (должен быть случайной строкой)

### 3. Выполнение миграций БД
- Убедитесь, что все SQL миграции выполнены в правильном порядке
- Проверьте структуру БД

### 4. Сборка проекта
```bash
npm run build
```

### 5. Запуск
```bash
# Для разработки
npm run dev

# Для продакшена
npm start
```

### 6. Первоначальная настройка
- [ ] Создайте первого админ пользователя (через регистрацию или SQL)
- [ ] Войдите как админ
- [ ] Настройте параметры платформы в `/settings`
- [ ] Создайте тестовый курс для проверки

## ⚠️ Важные моменты

### Безопасность
- ✅ Убедитесь, что `.env.local` не попал в git
- ✅ Используйте сильные секреты для `JWT_SECRET`
- ✅ Настройте HTTPS для продакшена
- ✅ Проверьте настройки CORS если нужно

### Производительность
- ✅ Настройте кеширование для статических файлов
- ✅ Используйте CDN для медиа файлов (рекомендуется)
- ✅ Настройте Redis для rate limiting (если используется)

### Мониторинг
- ✅ Настройте логирование ошибок
- ✅ Настройте мониторинг БД
- ✅ Проверьте доступность всех критичных endpoints

## 🧪 Тестирование после копирования

1. **Проверка базовых функций:**
   - [ ] Вход/выход работает
   - [ ] Дашборды открываются
   - [ ] Создание курса работает
   - [ ] Прохождение курса работает

2. **Проверка новых функций:**
   - [ ] Темная тема переключается
   - [ ] Видео плеер работает
   - [ ] Достижения начисляются
   - [ ] Обсуждения создаются
   - [ ] Уведомления приходят

3. **Проверка на разных устройствах:**
   - [ ] Работает на десктопе
   - [ ] Работает на планшете
   - [ ] Работает на мобильном

## 📝 Рекомендации

1. **Для продакшена:**
   - Используйте PM2 или systemd для управления процессом
   - Настройте автоматический перезапуск при ошибках
   - Настройте ротацию логов
   - Используйте reverse proxy (nginx/Apache)

2. **Для разработки:**
   - Используйте `npm run dev` для hot-reload
   - Настройте prettier/eslint для форматирования кода

3. **Резервное копирование:**
   - Настройте автоматическое резервное копирование БД
   - Регулярно проверяйте целостность данных

## 🆘 Если что-то не работает

1. Проверьте логи: `npm run dev` покажет ошибки в консоли
2. Проверьте подключение к БД: `DATABASE_URL` правильный?
3. Проверьте миграции: все таблицы созданы?
4. Проверьте переменные окружения: все необходимые настройки есть?
5. Проверьте консоль браузера: нет ли ошибок на клиенте?

## ✅ Готово!

После выполнения всех пунктов проект готов к использованию!

