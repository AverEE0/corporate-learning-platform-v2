# Настройка автоматического деплоя через GitHub

## ✅ Что уже сделано:

1. ✅ Git репозиторий инициализирован
2. ✅ GitHub remote добавлен
3. ✅ GitHub Actions workflow создан (`.github/workflows/deploy.yml`)
4. ✅ Код отправлен в GitHub

## 🔧 Что нужно настроить в GitHub:

### 1. Создайте репозиторий на GitHub (если еще не создан):
   - Перейдите на https://github.com/new
   - Название: `corporate-learning-platform-v2`
   - Сделайте репозиторий публичным или приватным
   - НЕ инициализируйте с README (код уже есть)

### 2. Настройте GitHub Secrets:

Перейдите в ваш репозиторий на GitHub:
- Settings → Secrets and variables → Actions → New repository secret

Добавьте следующие secrets:

#### SSH_HOST
```
212.113.123.94
```

#### SSH_USER
```
root
```

#### SSH_PRIVATE_KEY
Для получения приватного ключа выполните на сервере:
```bash
# Если ключа нет, создайте его:
ssh-keygen -t rsa -b 4096 -C "deploy@github" -f ~/.ssh/id_rsa_deploy -N ""

# Скопируйте приватный ключ:
cat ~/.ssh/id_rsa_deploy

# Добавьте публичный ключ в authorized_keys:
cat ~/.ssh/id_rsa_deploy.pub >> ~/.ssh/authorized_keys
```

Скопируйте содержимое `~/.ssh/id_rsa_deploy` и вставьте в секрет `SSH_PRIVATE_KEY` в GitHub.

### 3. Настройте сервер для автоматического деплоя:

Выполните на сервере:
```bash
cd /root/corporate-learning-platform-v2
git remote set-url origin https://github.com/omashi001/corporate-learning-platform-v2.git
git config --global credential.helper store
```

## 🚀 Как это работает:

1. Вы делаете изменения в коде локально
2. Делаете `git add .`, `git commit -m "описание"`, `git push`
3. GitHub Actions автоматически:
   - Клонирует код
   - Подключается к серверу по SSH
   - Обновляет код на сервере
   - Пересобирает Docker контейнер
   - Перезапускает приложение

## 📝 Команды для работы:

```bash
# Локально - отправить изменения
git add .
git commit -m "Описание изменений"
git push origin main

# На сервере - вручную обновить (если нужно)
cd /root/corporate-learning-platform-v2
git pull origin main
docker compose build app
docker compose up -d app
```

## ⚠️ Важно:

- Не коммитьте файлы с паролями (они в .gitignore)
- Все изменения автоматически деплоятся при push в main/master
- Проверяйте логи деплоя в GitHub Actions

