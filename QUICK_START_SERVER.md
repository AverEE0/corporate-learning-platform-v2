# 🚀 Быстрый запуск на сервере

## Автоматическая настройка (РЕКОМЕНДУЕТСЯ)

Выполните одну команду для полной настройки:

```bash
cd /root/corporate-learning-platform-v2
chmod +x setup-and-run.sh
./setup-and-run.sh
```

Скрипт автоматически:
- ✅ Проверит все зависимости
- ✅ Создаст необходимые файлы конфигурации
- ✅ Соберет Docker образы
- ✅ Запустит контейнеры
- ✅ Настроит базу данных
- ✅ Выполнит миграции

## Ручная настройка

Если предпочитаете ручную настройку:

### 1. Подключение к серверу

```bash
ssh root@212.113.123.94
# Пароль: wNaqg6r+wRUDV?
```

### 2. Переход в директорию проекта

```bash
cd /root/corporate-learning-platform-v2
```

### 3. Создание .env.local

```bash
cat > .env.local << EOF
DATABASE_URL=postgresql://postgres:password@postgres:5432/learning_platform
JWT_SECRET=tfj6T/jJ5dgTqoKfZmw1hTqJYSIXO/jI1g2RRlF87bE=
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0
NEXT_PUBLIC_APP_URL=http://212.113.123.94:3044
EOF
```

### 4. Запуск через Docker

```bash
# Создание директорий
mkdir -p uploads logs

# Остановка старых контейнеров
docker compose down

# Сборка и запуск
docker compose build --no-cache
docker compose up -d

# Проверка логов
docker logs -f corporate_learning_app
```

### 5. Проверка работы

```bash
# Проверка health endpoint
curl http://localhost:3044/api/health

# Проверка статуса контейнеров
docker compose ps
```

## Диагностика проблем

Если что-то не работает:

```bash
# Запустите диагностику
chmod +x diagnose-server.sh
./diagnose-server.sh

# Или автоматическое исправление
chmod +x fix-server.sh
./fix-server.sh
```

## Основные команды

```bash
# Статус контейнеров
docker compose ps

# Логи приложения
docker logs -f corporate_learning_app

# Логи базы данных
docker logs -f corporate_learning_db

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Полный перезапуск с пересборкой
docker compose down
docker compose build --no-cache
docker compose up -d
```

## Доступ к приложению

После успешного запуска приложение доступно по адресам:
- `http://localhost:3044` (на сервере)
- `http://212.113.123.94:3044` (внешний доступ, если порт открыт)

## Устранение неполадок

Подробное руководство см. в файле `SERVER_TROUBLESHOOTING.md`


