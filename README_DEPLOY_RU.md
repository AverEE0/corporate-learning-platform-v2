# 🚀 Развертывание Corporate Learning Platform на VPS

## 📋 Что включено

Проект полностью подготовлен для развертывания на VPS и включает:

✅ **Docker конфигурация**
- `Dockerfile` - production образ
- `Dockerfile.dev` - development образ
- `docker-compose.yml` - стандартная конфигурация
- `docker-compose.prod.yml` - с PostgreSQL в Docker

✅ **Nginx конфигурация**
- `nginx.conf` - готовый конфиг с rate limiting, gzip, security headers

✅ **Скрипты развертывания**
- `deploy.sh` - автоматическое развертывание (Linux/Mac)
- `deploy.ps1` - автоматическое развертывание (Windows)
- `scripts/setup-vps.sh` - автоматическая настройка VPS
- `scripts/init-db.sh` - инициализация БД
- `scripts/backup.sh` - резервное копирование
- `scripts/restore.sh` - восстановление из бэкапа

✅ **Документация**
- `START_HERE.md` - начните отсюда!
- `QUICK_DEPLOY.md` - быстрое развертывание за 5 минут
- `VPS_DEPLOYMENT.md` - полная инструкция
- `DEPLOYMENT.md` - детальное руководство
- `DEPLOY_CHECKLIST.md` - чеклист развертывания

✅ **Дополнительно**
- `Makefile` - удобные команды управления
- `health-check.sh` - проверка здоровья приложения
- `.env.production.example` - пример production конфигурации
- `.dockerignore` - оптимизация сборки

## ⚡ Быстрый старт

### 1. Подготовка сервера (5 минут)

```bash
# Установка Docker
curl -fsSL https://get.docker.com | sh
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
sudo usermod -aG docker $USER
newgrp docker
```

### 2. Загрузка проекта

```bash
cd /opt
mkdir -p corporate-learning-platform-v2
cd corporate-learning-platform-v2
# Загрузите все файлы проекта
```

### 3. Настройка

```bash
cp .env.production.example .env
nano .env  # Заполните переменные
```

### 4. Развертывание

```bash
chmod +x deploy.sh
./deploy.sh
```

### 5. Готово!

Откройте `http://your-server-ip` в браузере.

## 📝 Минимальный .env

```env
DATABASE_URL=postgresql://user:password@host:5432/corporate_learning
JWT_SECRET=$(openssl rand -base64 32)
NEXT_PUBLIC_APP_URL=http://your-server-ip
UPLOAD_DIR=/app/uploads
NODE_ENV=production
```

## 🛠 Управление

### Использование Makefile

```bash
make help      # Список команд
make build     # Собрать образы
make up        # Запустить
make down      # Остановить
make logs      # Просмотр логов
make restart   # Перезапуск
make backup    # Резервное копирование
```

### Использование Docker Compose

```bash
docker-compose up -d          # Запуск
docker-compose down           # Остановка
docker-compose logs -f app    # Логи
docker-compose restart app    # Перезапуск
docker-compose ps             # Статус
```

## 🔒 Безопасность

1. **Измените JWT_SECRET** на случайную строку (минимум 32 символа)
2. **Используйте сильные пароли** для БД
3. **Настройте firewall** (UFW)
4. **Установите SSL** (Let's Encrypt)
5. **Регулярно обновляйте** систему

## 💾 Резервное копирование

```bash
# Автоматический бэкап
./scripts/backup.sh

# Восстановление
./scripts/restore.sh backup_file.sql.gz
```

## 📊 Мониторинг

```bash
# Health check
curl http://localhost/api/health

# Логи
docker-compose logs -f app

# Статус
docker-compose ps
docker stats
```

## 🔄 Обновление

```bash
# Остановка
docker-compose down

# Обновление кода
git pull  # или загрузите новые файлы

# Пересборка и запуск
docker-compose build --no-cache
docker-compose up -d
```

## ❓ Решение проблем

### Приложение не запускается

```bash
# Проверьте логи
docker-compose logs app

# Проверьте переменные окружения
docker-compose exec app env | grep DATABASE_URL
```

### Проблемы с БД

```bash
# Проверьте подключение
docker-compose exec app ping postgres
```

### Проблемы с файлами

```bash
# Проверьте права
ls -la uploads/
chmod 755 uploads/
```

## 📚 Дополнительная документация

- **Начните здесь**: `START_HERE.md`
- **Быстрое развертывание**: `QUICK_DEPLOY.md`
- **Полная инструкция**: `VPS_DEPLOYMENT.md`
- **Детальное руководство**: `DEPLOYMENT.md`
- **Чеклист**: `DEPLOY_CHECKLIST.md`

## ✅ Чеклист после развертывания

- [ ] Приложение доступно по IP/домену
- [ ] База данных инициализирована
- [ ] SSL сертификат установлен (если используется домен)
- [ ] Firewall настроен
- [ ] Резервное копирование настроено
- [ ] Все пароли изменены
- [ ] Health check проходит

## 🎉 Готово!

Проект полностью готов к развертыванию на VPS!

