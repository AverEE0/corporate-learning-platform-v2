# 🎯 ИНСТРУКЦИЯ ДЛЯ СЕРВЕРА - НАЧНИТЕ ЗДЕСЬ!

## ⚡ Быстрый запуск (1 команда)

**Подключитесь к серверу и выполните:**

```bash
ssh root@212.113.123.94
# Пароль: wNaqg6r+wRUDV?

cd /root/corporate-learning-platform-v2
chmod +x setup-and-run.sh && ./setup-and-run.sh
```

**Готово!** Скрипт автоматически все настроит и запустит.

---

## 📋 Что делает скрипт

1. ✅ Проверяет установку Docker и docker-compose
2. ✅ Создает файл `.env.local` с правильными настройками
3. ✅ Проверяет и исправляет `next.config.js`
4. ✅ Создает необходимые директории (uploads, logs)
5. ✅ Останавливает старые контейнеры
6. ✅ Собирает Docker образы без кеша
7. ✅ Запускает контейнеры (PostgreSQL + приложение)
8. ✅ Создает базу данных если нужно
9. ✅ Выполняет SQL миграции
10. ✅ Проверяет работоспособность

---

## 🔍 После запуска скрипта

### Проверка работы

```bash
# Статус контейнеров
docker compose ps

# Логи приложения
docker logs -f corporate_learning_app

# Проверка health endpoint
curl http://localhost:3044/api/health
```

### Доступ к приложению

- **На сервере**: `http://localhost:3044`
- **Внешний доступ**: `http://212.113.123.94:3044`

---

## 🐛 Если что-то не работает

### Вариант 1: Диагностика

```bash
chmod +x diagnose-server.sh
./diagnose-server.sh
```

Скрипт покажет все проблемы и рекомендации.

### Вариант 2: Автоматическое исправление

```bash
chmod +x fix-server.sh
./fix-server.sh
```

### Вариант 3: Ручное исправление

См. подробное руководство: `SERVER_TROUBLESHOOTING.md`

---

## 📚 Основные команды

```bash
# Статус
docker compose ps

# Логи
docker logs -f corporate_learning_app

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Полная пересборка
docker compose down
docker compose build --no-cache
docker compose up -d
```

---

## ⚠️ Важные замечания

1. **Пароль БД**: По умолчанию используется `password`. Если у вас другой пароль, обновите `.env.local` и `docker-compose.yml`

2. **Порт**: Приложение работает на порту **3044**. Убедитесь, что он открыт в firewall.

3. **База данных**: Если используете внешнюю БД, обновите `DATABASE_URL` в `.env.local`

4. **Домен**: Если используете домен, обновите `NEXT_PUBLIC_APP_URL` в `.env.local`

---

## 📞 Полезные файлы

- `QUICK_START_SERVER.md` - Краткая инструкция
- `SERVER_TROUBLESHOOTING.md` - Подробное руководство по устранению неполадок
- `diagnose-server.sh` - Скрипт диагностики
- `fix-server.sh` - Скрипт автоматического исправления
- `setup-and-run.sh` - Главный скрипт установки

---

## ✅ Чеклист успешного запуска

- [ ] Docker и docker-compose установлены
- [ ] Файл `.env.local` создан
- [ ] Контейнеры запущены (`docker compose ps`)
- [ ] Health endpoint отвечает (`curl http://localhost:3044/api/health`)
- [ ] База данных создана и миграции выполнены
- [ ] Приложение доступно в браузере

---

**Удачи! 🚀**


