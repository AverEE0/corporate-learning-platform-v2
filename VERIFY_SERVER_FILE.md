# Проверка файла на сервере

Проблема: Изменения не применяются.

Нужно проверить:
1. Что файл `app/admin/dashboard/page.tsx` на сервере содержит правильный код
2. Что условие `if (loading && !user)` присутствует (строка 71)
3. Что `ThemeToggle` и `NotificationsBell` присутствуют в JSX (строки 111-112)
4. Что BUILD_ID изменился после пересборки

Если файл не обновлен - нужно использовать другой способ передачи файла (например, scp или rsync).


